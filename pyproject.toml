[project]
name = "public-record-versioned"
version = "0.1.0"
description = "A bounded viability spike for evidence-linked government report version history."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
